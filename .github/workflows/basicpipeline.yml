name: Composite java action
on:
  push:
    branches: ["master"]
jobs:
  build:
    runs-on: ubuntu-latest  
    steps:
      - name: Build step
        run: echo "Hello, ${{ secrets.DOCKER_PASSWORD }}"
